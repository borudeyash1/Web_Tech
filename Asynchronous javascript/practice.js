// async function f() {
//     let promise = new Promise((resolve, reject) => {
//         setTimeout(() => resolve("done!"), 3000);
//     });
//     let result = await promise; // wait until the promise resolves (*)
//     console.log(result); // "done!"
//     console.log("Async function executed");
//     return result; // return the value
// }
// f(); // call the async function


/*write a javascript program to fetch the data from the public api using the fetch and async-await
display the result dynamically on a web page using DOM manipulation*/
// async function waitAndResponsd(){
//     await new Promise(resolve => setTimeout(resolve, 1000));
//     return "Response after 3 seconds";
// }

// //usage
// waitAndResponsd().then(console.log);

/* Step 1:Define an async fucntion to fetch the */ 


